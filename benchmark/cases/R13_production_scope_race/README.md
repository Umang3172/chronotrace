# R13 — production_scope_race

**Shape:** the inverted pair lives in `app_service.py`, which is product code. ChronoTrace reports scope and abstains unless `--allow-production-repair` is passed (INV-7 / S2).
