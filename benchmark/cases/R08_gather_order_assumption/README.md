# R08 — gather_order_assumption

**Shape:** over-constrained assertion. Two independent fetches may complete in either order; the test insists on one. The correct output is a recommendation to relax the assertion, held for human review — never an automatic assertion edit.
