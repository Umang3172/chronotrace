# R12 — depth2_two_constraints

**Shape:** depth-2 race. Failure requires two orderings simultaneously. Forcing either alone gives a failure rate strictly between 0 and 1, which classifies the candidate NECESSARY_INSUFFICIENT. Expected outcome: NEEDS INVESTIGATION with the depth reported, not a patch.
