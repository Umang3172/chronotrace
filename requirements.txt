# AgentCore Runtime bundle manifest. This is NOT how you install ChronoTrace for
# development -- use `uv sync --extra bedrock --extra dev` for that, driven by
# pyproject.toml and uv.lock, which remain the source of truth.
#
# It exists because the AgentCore starter toolkit resolves dependencies for a
# direct-code deploy by looking for a requirements.txt at the bundle root, and
# cross-compiles them for linux/arm64 with `uv pip install --only-binary :all:`.
# Under that flag the local project cannot be installed from source, so the
# dependency set is listed explicitly here and `chronotrace` itself is imported
# from the bundle rather than installed. Keep this in step with the
# `dependencies` and `bedrock` extra in pyproject.toml.
bedrock-agentcore

# chronotrace runtime dependencies (pyproject.toml [project.dependencies])
pydantic>=2.7
pydantic-settings>=2.3
libcst>=1.4
networkx>=3.3
structlog>=24.1
typer>=0.12

# pytest and its plugins are runtime dependencies here, not test-only: the
# agent's capture and forced-replay tools spawn `python -m pytest` subprocesses.
pytest>=8.2
pytest-asyncio>=0.23
pytest-json-report>=1.5
pytest-repeat>=0.9

# pyproject.toml [project.optional-dependencies] bedrock
boto3>=1.34
strands-agents>=0.1
